# --- 1. 自动生成的动态数据 (绝对核心) ---
# 忽略运行时导出与临时数据
**/exports/

# --- 2. 数据库文件 (全局清理) ---
# 无论在根目录还是 data 目录下的 .db 都要排除
**/*.db

# --- 3. 配置文件与隐私 (防泄漏) ---
**/config.yaml
!**/config.default.yaml

# --- 4. 前端开发黑洞 (必须添加) ---
# 看到你目录下有 node_modules，这个绝对不能传！
**/node_modules/
# 排除 Vite/Webpack 的构建产物
**/dist/
# 排除 TypeScript 构建缓存
*.tsbuildinfo

# --- 5. 冗余与备份 ---
old_version/
**/__pycache__/
*.log
battle_report/
plan/
tests/
temp_*.png

# --- 6. 统一 Web 应用运行时 ---
.omx/
.runtime/
uploads/
**/.venv/
**/venv/
**/env/
**/.env
openai_providers.json
**/*.py[cod]
Z.R.I.C.log*
.multiplayer-server.*.log
.unified-server.*.log
