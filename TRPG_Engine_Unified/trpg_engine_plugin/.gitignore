# --- 1. 自动生成的动态数据 (绝对核心) ---
# 忽略所有层级的角色数据文件夹
**/chara_data/
**/chara_data_/
**/user_metadata/
# 忽略跑团日志和导出文件
**/group_logs/
**/exports/
data/settings/

# --- 2. 数据库文件 (全局清理) ---
# 无论在根目录、data 还是 V20 目录下的 .db 都要排除
**/*.db
!V20_char_sheet/setup.sql

# --- 3. 配置文件与隐私 (防泄漏) ---
**/config.yaml
!**/config.default.yaml
# 排除前端的开发环境变量
log-painter/frontend/.env*

# --- 4. 前端开发黑洞 (必须添加) ---
# 看到你目录下有 node_modules，这个绝对不能传！
**/node_modules/
# 排除 Vite/Webpack 的构建产物
**/dist/
# 排除 TypeScript 构建缓存
*.tsbuildinfo

# --- 5. 冗余与备份 ---
old_version/
**/__pycache__/
*.log
plan/
tests/
log-painter/frontend/test-fixtures/
temp_*.png
